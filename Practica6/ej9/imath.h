#ifndef IMATH_H
#define IMATH_H

// Función para calcular x elevado a la potencia y
int potencia(int x, int y);

// Función para calcular el cuadrado de x
int cuadrado(int x);

// Función para calcular el cubo de x
int cubo(int x);

// Función para obtener el valor absoluto de un entero
int absoluto(int i);

// Función para calcular el factorial de un número
int factorial(int i);

// Función para calcular la sumatoria desde 0 hasta i
int sumatoria(int i);

// Función para comprobar si y es múltiplo de x
int multiplo(int x, int y);

// Función para comprobar si y es divisor de x
int divisor(int x, int y);

// Función para comprobar si un número es par
int par(int i);

// Función para comprobar si un número es impar
int impar(int i);

#endif /* IMATH_H */
